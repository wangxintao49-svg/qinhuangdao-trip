# 秦皇岛双站旅行地图 —— 项目完全文档

## 一、项目概述

这是一个秦皇岛旅游地点的交互式地图网站，覆盖 **北戴河站 → 秦皇岛站** 双站之间的景点和美食。包含地图浏览、路线规划、AI 行程推荐、多日行程管理等功能。支持 **网页版、Windows EXE、安卓 APK** 三种形态。

---

## 二、技术栈

| 层次 | 技术 | 版本 |
|------|------|------|
| **框架** | React | 19.x |
| **语言** | TypeScript | 6.x |
| **构建工具** | Vite | 8.x |
| **CSS** | Tailwind CSS | 3.x |
| **状态管理** | Zustand | 5.x |
| **路由** | react-router-dom | 7.x |
| **地图** | 腾讯地图 GL JS SDK | v2.exp |
| **AI** | DeepSeek API（chat completions） | — |
| **数据库** | Supabase（PostgreSQL 云端） | — |
| **语音** | 百度 TTS → 浏览器降级 | — |
| **图标** | lucide-react | — |
| **图片导出** | html2canvas | — |
| **桌面打包** | Electron + electron-packager | — |
| **移动打包** | Capacitor（Android） | — |
| **部署** | GitHub Pages + GitHub Actions | — |

---

## 三、API 密钥与凭证

### 腾讯地图
- **用途**：地图显示、地点搜索（Suggestion）、周边搜索（Search）、路线规划（Direction）
- **JS SDK Key**：`Z7ZBZ-LIIKZ-FAIXT-7FPTG-4EYWS-LDFXH`
- **加载方式**：`https://map.qq.com/api/gljs?v=2.exp&key=KEY&libraries=service`
- **API 地址**：`https://apis.map.qq.com/ws/...`
  - 地点建议：`/ws/place/v1/suggestion/`
  - 周边搜索：`/ws/place/v1/search/`
  - IP 定位：`/ws/location/v1/ip`
  - 路线规划：`/ws/direction/v1/{driving|walking|bicycling|transit}/`

### DeepSeek AI
- **用途**：AI 智行页面，智能旅行规划对话
- **API Key**：`sk-0fcf0ef58fd44d3ca1b214abd5bcf844`
- **API 地址**：`https://api.deepseek.com/v1/chat/completions`
- **模型**：`deepseek-chat`
- **价格**：约 ¥0.2 / 百万 tokens

### Supabase（数据库）
- **用途**：存储景点数据、避坑数据、用户收藏
- **项目地址**：`https://mzfeqmaaqpllgehishai.supabase.co`
- **匿名 Key**：`eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6Im16ZmVxbWFhcXBsbGdlaGlzaGFpIiwicm9sZSI6ImFub24iLCJpYXQiOjE3NzczNjQxNjMsImV4cCI6MjA5Mjk0MDE2M30.T8fNfeK2bgqy67bmcUoCkvl6ZXPwlY-SfwNKCEQXN0E`
- **数据表**：
  - `spots` — 景点/美食/车站
  - `pitfalls` — 避坑档案
  - `wishlists` — 用户收藏（按 session_id）

### 百度 TTS（语音合成）
- **用途**：PageIntro 页面语音介绍（可选，未配置时自动降级浏览器自带语音）
- **API Key**：`cQ4jAPPYihsfG75aLbxauIzO`
- **Secret Key**：`5XWBkDIa5fmixdtJWo6IGYtqY69gx3Cj`
- **认证地址**：`https://aip.baidubce.com/oauth/2.0/token`
- **合成地址**：`https://tsn.baidu.com/text2audio`

---

## 四、GitHub 账号信息

| 项目 | 内容 |
|------|------|
| **GitHub 用户名** | wangxintao49-svg |
| **仓库** | `qinhuangdao-trip` |
| **Token 名称** | push-code（repo + workflow 权限） |
| **网站地址** | `https://wangxintao49-svg.github.io/qinhuangdao-trip/` |
| **本地目录** | `D:\秦皇岛之旅\` |

---

## 五、项目目录结构

```
D:\秦皇岛之旅\
├── src/
│   ├── pages/           # 页面组件（每个页面一个文件）
│   │   ├── MapView.tsx       # 总览地图
│   │   ├── RoutePlanner.tsx  # 路线规划
│   │   ├── TripPlan.tsx      # 我的行程
│   │   ├── PlaceGallery.tsx  # 地点库
│   │   ├── AIChat.tsx        # AI 智行
│   │   ├── PitfallArchive.tsx# 避坑档案
│   │   └── AppSettings.tsx   # 设置
│   ├── components/      # 复用组件
│   │   ├── PageIntro.tsx     # 页面语音引导弹窗
│   │   └── ...
│   ├── services/        # API 服务
│   │   ├── api.ts            # 腾讯地图 SDK + DeepSeek
│   │   ├── supabase.ts       # Supabase 数据库
│   │   └── speak.ts          # 语音合成
│   ├── store/           # 状态管理
│   │   └── tripStore.ts      # Zustand 全局状态
│   ├── data/            # 静态数据
│   │   ├── places.ts         # 22个地点的完整数据（名称、坐标、介绍）
│   │   └── pageIntros.ts     # 页面引导语音文案
│   ├── utils/           # 工具函数
│   │   ├── geo.ts            # 地理距离/费用估算
│   │   ├── speak.ts          # TTS 语音
│   │   ├── icons.ts          # 地图标记图标生成
│   │   └── assets.ts         # 静态资源路径适配
│   ├── types/           # TypeScript 类型定义
│   │   └── global.d.ts       # TMap SDK 类型声明
│   ├── layouts/         # 布局
│   │   └── MainLayout.tsx    # 桌面顶栏 + 手机底栏
│   ├── App.tsx          # 路由配置
│   └── main.tsx         # 入口文件
├── public/
│   └── images/          # 22个地点的实景照片
├── electron/
│   ├── main.cjs         # Electron 主进程
│   └── preload.cjs      # 预加载脚本
├── .github/
│   └── workflows/
│       ├── deploy-pages.yml  # GitHub Pages 自动部署
│       └── build-apk.yml     # 安卓 APK 自动构建
├── capacitor.config.ts  # Capacitor 配置
├── vite.config.ts       # Vite 构建配置
├── package.json         # 项目依赖和脚本
└── .env                 # 本地环境变量
```

---

## 六、所有功能列表

| 功能 | 页面 | 说明 |
|------|------|------|
| 地图标记 | 地图 | 22个地点用彩色图标标注，白色标签显示名称 |
| 分类筛选 | 地图 | 全部/玩乐/吃喝/车站/雨天 |
| 地点搜索 | 地图 | 腾讯地图 API 自动补全 |
| 地点详情 | 地图 | 图片、介绍、标签、周边探索 |
| 路况图层 | 地图 | 开关实时交通 |
| 实时定位 | 地图/路线 | GPS 定位 + 蓝点标记 + 跟随模式 |
| 路线规划 | 路线 | 驾车/骑行/公交/步行四种方式 |
| 路线图 | 路线 | 距离/耗时/费用展示，公交含详细步骤 |
| 地点库 | 地点库 | 22个地点完整数据，评分/标签/避坑提示 |
| 收藏 | 地点库/地图 | ❤️ 加入/取消收藏，云端同步 |
| AI 规划 | AI 智行 | DeepSeek 对话，自动生成路线方案 |
| 路线预览 | AI 智行 | 右侧面板显示推荐路线时间线 |
| 应用到行程 | AI 智行 | AI 方案一键导出到行程页 |
| 多日行程 | 行程 | 创建多天，拖拽排序，跨天移动 |
| 出行方式 | 行程 | 每段路线独立选驾车/骑行/公交/步行 |
| 时间推算 | 行程 | 自动推算到达时间 |
| 路线图 | 行程 | 在地图上绘制完整行程轨迹 |
| 图片导出 | 行程 | html2canvas 导出为 PNG |
| 智能排序 | 行程 | 最近邻优化游览顺序 |
| 避坑档案 | 避坑 | 12个被剔除地点，标签筛选 |
| 页面引导 | 所有页面 | 首次访问弹出语音介绍弹窗 |
| 收藏管理 | 设置 | 数据概览、JSON 导出 |
| 响应式布局 | 全局 | 电脑/手机自适应 |
| 离线桌面版 | EXE | Electron 打包，双击运行，81MB |
| 离线手机版 | APK | Capacitor 打包，安卓安装 |

---

## 七、核心开发命令

```bash
# 本地开发
npm run dev          # 启动开发服务器

# 构建
npm run build        # 编译前端（tsc + vite build）

# 打 EXE 包（需 Electron）
npx vite build
npx electron-packager . "秦皇岛之旅" --platform=win32 --arch=x64 --out=./output

# APK 包
# 在 GitHub Actions 中自动构建，或：
npx cap add android
npx cap sync
cd android && ./gradlew assembleDebug

# 推送到 GitHub
git add -A
git commit -m "描述你的修改"
git push origin master
```

---

## 八、部署流程

### 网页版（GitHub Pages，免费）
1. 修改代码 → `git push`
2. GitHub Actions 自动运行 `deploy-pages.yml`
3. 构建 → 部署到 Pages
4. 访问：`https://wangxintao49-svg.github.io/qinhuangdao-trip/`

### 桌面版（EXE，免费）
1. 改代码 → `npm run build`（构建前端）
2. `npx electron-packager . "秦皇岛之旅" --platform=win32 --arch=x64 --out=./output`
3. 得到可移植文件夹 → 发 ZIP 或打包成安装程序

### 手机版（APK，免费）
1. 改代码 → `git push`
2. GitHub Actions 自动运行 `build-apk.yml`
3. 构建完成后 → Actions → 下载 APK 文件
4. 发给用户安装

---

## 九、数据来源与坐标校准

- 地点数据：22个地点，存储在 `src/data/places.ts`
- 图片：存储在 `public/images/`（实景照片）
- 坐标：通过**腾讯地图地址解析 API** 校准
  - API：`https://apis.map.qq.com/ws/geocoder/v1/?address=地址&key=KEY`
  - 如果坐标不准，重新调用此 API 获取最新坐标
- 坐标系：**GCJ-02**（火星坐标系，腾讯/高德通用）

---

## 十、关键经验与注意事项

1. **CORS 跨域问题**：腾讯地图的 REST API 不支持浏览器直连（无 CORS 头）。需要：
   - 本地开发：Vite proxy 转发
   - Electron/EXE：Express 本地代理
   - APK：WebView 配置 `setAllowUniversalAccessFromFileURLs(true)`
   - GitHub Pages：无法解决，只能用 TMap SDK 内置服务 + Haversine 估算降级

2. **TMap SDK 与 REST API 的关系**：
   - `TMap.service.Suggestion` / `TMap.service.Search`：SDK 内置，无 CORS 问题
   - `TMap.service.Direction`：GL JS SDK **不包含**，只能走 REST API
   - 解决方案：用 `libraries=service` 参数加载 SDK + REST API 兜底

3. **GitHub Pages SPA 路由**：
   - 必须设置 `vite.config.ts` 的 `base: '/仓库名/'`
   - 必须设置 `<BrowserRouter basename="/仓库名">`
   - 必须把 `index.html` 复制为 `404.html`（处理刷新 404）

4. **静态资源路径**：GitHub Pages 下所有资源路径需加 base 前缀（`/仓库名/images/`），写了一个 `imgUrl()` 工具函数统一处理

5. **打包成 EXE**：
   - Electron 需加载 `libraries=service` 的 SDK
   - 用 `express.static('dist')` + SPA fallback
   - 不签名的话首次运行会弹 Windows 蓝色警告

6. **打包成 APK**：
   - 用 Capacitor + GitHub Actions
   - 需要 Java 21 + Android SDK
   - 需要 `tweetsodium` 库加密 GitHub Secrets

7. **GitHub Token 权限**：
   - `repo` 权限：推送代码
   - `workflow` 权限：创建工作流文件
   - Token 要去 GitHub Settings → Developer settings → Tokens 管理

---

## 十一、如果你以后要问 AI 做类似项目

告诉 AI 以下信息，它就能直接上手：

> 我有一个 React + TypeScript + Vite + Tailwind 的项目，状态管理用 Zustand，路由用 react-router-dom。地图用腾讯地图 GL JS SDK（key: Z7ZBZ-LIIKZ-FAIXT-7FPTG-4EYWS-LDFXH）。AI 用 DeepSeek（key: sk-0fcf0ef58fd44d3ca1b214abd5bcf844）。数据库用 Supabase（地址: https://mzfeqmaaqpllgehishai.supabase.co）。部署在 GitHub Pages（用户名 wangxintao49-svg，仓库 qinhuangdao-trip）。打包桌面用 Electron，打包手机用 Capacitor。

---

> *文档生成时间：2026年4月29日*
> *后续如有 API Key 更换或新增功能，请同步更新本文档*

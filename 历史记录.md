# 秦皇岛之旅 · 完整项目历史

> 最后更新：2026-04-27

---

## 一、项目基本信息

| 项目 | 内容 |
|------|------|
| 项目名称 | 秦皇岛之旅（双站旅行地图） |
| 旅行范围 | 北戴河站 → 秦皇岛站（沿渤海海岸线） |
| 用户 | 王新涛 |
| 用户位置 | 山西省（不在秦皇岛本地，开发时需注意测试定位逻辑） |
| 技术栈 | React 19 + TypeScript 5 + Vite 8 + Tailwind CSS 3 |
| 地图 SDK | 腾讯地图 GL JS API v2.exp（`TMap` 命名空间） |
| AI 服务 | DeepSeek Chat API |
| 语音服务 | 百度 TTS（在线语音合成） |
| 包管理 | npm |
| 开发端口 | 3002 |
| 测试浏览器 | Microsoft Edge |

---

## 二、用户的全部需求和沟通过程

### 2.1 第一阶段：项目创建和基础功能

**用户需求：** 基于策划书重新开发秦皇岛旅行网站，从零开始。

**完成内容：**
- 创建 React + Vite 项目，配置 Tailwind 和腾讯地图
- 录入景点数据（32个地点，含车站、玩乐、吃喝、雨天备选）
- 录入避坑数据（12个被剔除的地点）
- 搭建 8 个页面的路由
- 总览地图：地图展示、分类筛选、地点详情、周边探索
- 路线规划：驾车/骑行/公交/步行四种模式
- 地点库：按分类浏览、搜索、收藏
- 避坑档案：按风险标签筛选
- 设置页：功能开关、数据导出

### 2.2 第二阶段：公交路线修复

**用户需求：** "你把这个公交车这个给我修复好，我要从特别详细的信息从哪上车从哪下车从哪换乘预计多久坐哪一路"

**问题：** 之前公交模式调用腾讯地图 Direction API 后，无法正确解析返回值。API 返回的 polyline 在嵌套的 steps 里（WALKING 和 TRANSIT 混合），不是直接在 route 层。

**解决方案：**
- 新增 `parseTransitRoute()` 函数解析嵌套步骤
- 提取 transit 模式的线路名（title）、上车点（geton）、下车点（getoff）、途经站点列表（stations）
- WALKING 段显示步行方向和分段指引
- 新增 `drawTransitRoute()` 函数：公交段用蓝色 #0891B2 画线，步行段用灰色 #94A3B8 画线
- 右侧结果卡片显示公交票价

**腾讯地图 Direction API（公交模式）返回结构：**
```
result.routes[0].steps[]
  ├── WALKING: { instruction, polyline, distance, duration }
  └── TRANSIT: { distance, duration, polyline, lines[]
        └── { title(线路名), geton/getoff(上下车站点), station_count, stations[]
              └── { name, location } } }
```

### 2.3 第三阶段：行程管理改造

**用户需求：** 
1. "在行程里面去查看两个节点之间的信息太少了" 
2. "不同的交通方式所给出的信息"
3. "轨迹是直线的，并不像咱们的行程规划也是一个真实的路径"
4. "我也看不到我自己的位置"

**解决方案：**
- TripPlan 新增交通模式选择器（driving/bicycling/transit/walking）
- 每段行程分别调用 Direction API 获取真实路线数据
- 真实轨迹 polyline 替代原来的直线（haversine 估算降级）
- 新增用户位置标记（蓝色原点 + 精度圈）
- 新增位置追踪和跟随模式
- 新增 decodeDirectionPolyline() 解码腾讯地图的 delta 编码 polyline 格式

**腾讯地图 polyline 编码格式（重要）：**
```typescript
// 平铺的 number 数组
// 第一对：绝对坐标（度）
// 后续对：delta 微度（×0.000001）
function decodeDirectionPolyline(data: number[]): Array<{lat, lng}> {
  let lat = data[0], lng = data[1]
  for (let i = 2; i < data.length - 1; i += 2) {
    lat += data[i] * 0.000001
    lng += data[i + 1] * 0.000001
  }
}
```

### 2.4 第四阶段：位置服务完善

**用户需求：**
1. "定位到几十米，我要定位到一米"（想要高精度）
2. "在所有地图中，右下角点一下自动跳转到我的位置"
3. "做一个跟随模式，地图随着我的移动自动移动"

**解决方案：**
- 新增 `getUserLocation()`：优先浏览器 GPS/WiFi（`enableHighAccuracy: true`），超时 10 秒降级到 IP 定位
- 新增 `watchUserLocation()`：持续追踪位置变化
- 三个地图页面（MapView、RoutePlanner、TripPlan）都添加了：
  - 📍 定位按钮（点击跳到用户位置）
  - 🎯 跟随模式（地图自动跟随位置移动）
  - 蓝色精度圈（显示定位精度范围）
- `getIpLocation()`：调用腾讯地图 IP 定位 API 做降级

**腾讯地图 IP 定位 API：**
```
接口：GET /ws/location/v1/ip?key={key}
返回：{ status: 0, result: { location: { lat, lng }, ad_info: { city } } }
代理：/tencent-api/ws/location/v1/ip?key={key}
```

### 2.5 第五阶段：语音导航系统

**用户需求：**
1. "用户打开某一页之后，它会介绍这一页有什么功能，自动播报"
2. "显示一个类似于图片的东西在页面，用户点击确认之后就取消"
3. "浏览器自带语音很难听，能不能用其他方式弄成更好听的声音"

**解决方案：**
- 新建 `PageIntro` 组件：全屏浮层 + 图标 + 标题 + 介绍文字 + 语音播放按钮
- 8 个页面各自配置介绍文案（`pageIntros.ts`）
- 集成百度 TTS API（高质量语音）
- 浏览器原生语音降级（SpeechSynthesis）
- 自动播放语音（被浏览器 autoplay policy 拦截时显示手动播放按钮）
- `sessionStorage` 记录已读状态（关标签页重开后重新显示）

**百度 TTS 集成过程：**
1. 用户提供百度智能云 TTS 的 API Key 和 Secret Key
2. 通过百度 OAuth 2.0 获取 access_token
3. 调用 text2audio 接口合成语音

**百度 TTS API 详情：**
```
获取 Token：
  POST https://aip.baidubce.com/oauth/2.0/token
  Body: grant_type=client_credentials&client_id={key}&client_secret={secret}
  代理：/baidu-auth/oauth/2.0/token

语音合成：
  POST https://tsn.baidu.com/text2audio
  Body (URLSearchParams):
    tex: 要合成的文本
    tok: access_token
    cuid: qhd_trip_web
    ctp: 1
    lan: zh
    spd: 5（语速）
    pit: 5（音调）
    vol: 5（音量）
    per: 4134（发音人）
    aue: 3（MP3 格式）
  成功返回：二进制音频（Content-Type: audio/*）
  失败返回：JSON（Content-Type: application/json）
  代理：/baidu-tts/text2audio
```

### 2.6 地图标记图标问题

**用户需求：** "各个景点做一下区分，吃喝一个类别，玩的一个类别，车站一个类别，不同的图标。我的位置就是家的位置，一个房子图标。"

**尝试过的方案（均失败）：**
1. **SVG data URI** → 腾讯地图 WebGL 不支持
2. **SVG 文件（/public/icons/）** → 同样是矢量格式，WebGL 纹理无法加载
3. **Canvas 生成 PNG data URL** → 理论可行，但被无限重建 bug 覆盖，未验证

**当前状态：** `src/utils/icons.ts` 已实现 Canvas 生成方案，等待 3.1 bug 修复后验证。

---

## 三、API 接口完整清单

### 3.1 腾讯位置服务 API（通过 Vite proxy）

| 用途 | 接口 | 代理路径 | 说明 |
|------|------|----------|------|
| 地图 SDK | `https://map.qq.com/api/gljs?v=2.exp&key={key}` | 直接引用 | 动态加载 script |
| 地点建议 | `GET /ws/place/v1/suggestion/?keyword=&region=&key=` | `/tencent-api` | 搜索框自动补全 |
| 周边搜索 | `GET /ws/place/v1/search/?boundary=nearby(lat,lng,500)&key=` | `/tencent-api` | 500m 范围 POI |
| IP 定位 | `GET /ws/location/v1/ip?key=` | `/tencent-api` | GPS 降级用 |
| 路线规划 | `GET /ws/direction/v1/{mode}/?from=&to=&key=` | 直接请求 | mode=driving/bicycling/transit/walking |

### 3.2 百度 AI 平台 API

| 用途 | 接口 | 代理路径 |
|------|------|----------|
| OAuth 鉴权 | `POST https://aip.baidubce.com/oauth/2.0/token` | `/baidu-auth` |
| 语音合成 | `POST https://tsn.baidu.com/text2audio` | `/baidu-tts` |

### 3.3 DeepSeek API

| 用途 | 接口 |
|------|------|
| AI 对话 | `POST https://api.deepseek.com/v1/chat/completions` |

### 3.4 本地 Web Speech API（降级方案）

| 功能 | 接口 |
|------|------|
| 语音合成 | `window.speechSynthesis.speak(new SpeechSynthesisUtterance(text))` |
| 获取语音列表 | `window.speechSynthesis.getVoices()` |

---

## 四、API 密钥（已配置，切勿泄露）

```
腾讯地图:     VITE_TENCENT_KEY=Z7ZBZ-LIIKZ-FAIXT-7FPTG-4EYWS-LDFXH
DeepSeek AI:  VITE_DEEPSEEK_KEY=sk-0fcf0ef58fd44d3ca1b214abd5bcf844
百度 TTS Key: VITE_BAIDU_TTS_KEY=cQ4jAPPYihsfG75aLbxauIzO
百度 TTS Secret: VITE_BAIDU_TTS_SECRET=5XWBkDIa5fmixdtJWo6IGYtqY69gx3Cj
```

---

## 五、Vite Proxy 配置

```typescript
proxy: {
  '/tencent-api': {
    target: 'https://apis.map.qq.com',
    changeOrigin: true,
    rewrite: (p) => p.replace(/^\/tencent-api/, ''),
  },
  '/baidu-auth': {
    target: 'https://aip.baidubce.com',
    changeOrigin: true,
    rewrite: (p) => p.replace(/^\/baidu-auth/, ''),
  },
  '/baidu-tts': {
    target: 'https://tsn.baidu.com',
    changeOrigin: true,
    rewrite: (p) => p.replace(/^\/baidu-tts/, ''),
  },
},
```

---

## 六、全部已完成功能清单

### 6.1 页面
- [x] 首页（HomePage）：项目介绍、快速导航
- [x] 总览地图（MapView）：32 个地点标记、分类筛选、搜索、周边探索、实时路况、用户定位
- [x] 路线规划（RoutePlanner）：起终点选择、驾车/骑行/公交/步行、真实轨迹、公交详细换乘方案
- [x] 行程管理（TripPlan）：收藏地点列表、拖拽排序、智能优化（TSP 算法）、多段真实路线、模式切换
- [x] 地点库（PlaceGallery）：所有景点清单、分类/搜索筛选、收藏操作
- [x] AI 助手（AIChat）：DeepSeek 驱动的旅游问答
- [x] 避坑档案（PitfallArchive）：按风险标签筛选的避坑记录
- [x] 设置页（AppSettings）：功能开关、服务状态、数据概览、JSON 导出

### 6.2 地图功能
- [x] 腾讯地图 GL JS SDK 动态加载
- [x] 所有地点以 MultiMarker 展示
- [x] 按玩乐/吃喝/车站/雨天分类筛选
- [x] 搜索框自动补全（本地 + 腾讯地图 API）
- [x] 选中地点详情卡片
- [x] 周边 POI 探索（500m 范围）
- [x] 实时路况图层（TrafficLayer）
- [x] IP 定位初始化（降级方案）

### 6.3 路线功能
- [x] 四种交通模式的 Direction API 调用
- [x] 真实 polyline 轨迹绘制（非直线）
- [x] 距离/耗时/费用计算
- [x] Haversine 公式降级估算
- [x] Transit 模式详细解析：线路名、上车点、下车点、途经站列表、步行指引、换乘衔接

### 6.4 定位功能
- [x] 浏览器 Geolocation API 获取位置
- [x] GPS/WiFi 高精度定位优先
- [x] 腾讯地图 IP 定位降级
- [x] watchPosition 实时位置追踪
- [x] 用户蓝色标记 + 精度圈
- [x] 跟随模式（地图随用户移动）
- [x] 三个地图页面各有一组定位控件

### 6.5 语音系统
- [x] 百度 TTS 高质量语音合成
- [x] 浏览器 SpeechSynthesis 降级
- [x] 8 个页面的引导介绍文案
- [x] PageIntro 全屏浮层组件
- [x] 自动播放（首次访问）
- [x] 手动播放按钮（autoplay 被拦截时）
- [x] sessionStorage 记忆已读状态

### 6.6 工具函数
- [x] haversineDist：地理距离计算
- [x] decodeDirectionPolyline：腾讯地图 delta polyline 解码
- [x] generateCurvedPath：两点间贝塞尔插值路径
- [x] estDriveTime/estTaxiFare/estWalkTime/estRideTime/estTransitTime：各模式时间和费用估算
- [x] getUserLocation：GPS→IP 分级定位
- [x] watchUserLocation：持续位置追踪
- [x] getMarkerIcon / getHouseIcon：Canvas 生成 PNG 标记图标（待验证）

---

## 七、已解决的历史问题

| 问题 | 根因 | 解决方案 |
|------|------|----------|
| Transit 模式报错"Cannot read properties of undefined" | API 返回的 polyline 在嵌套 steps 里，不在 route 层 | 分支处理 transit 模式，用 parseTransitRoute 提取 |
| 浏览器 CORS 拦截腾讯地图 API 请求 | 前端直接请求 apis.map.qq.com | Vite proxy 配置 `/tencent-api` 转发 |
| IP 定位显示错误城市（寿阳县） | IP 定位精度只有城市级 | 改为浏览器 Geolocation 优先，IP 定位仅做降级 |
| 浏览器自动播放被拦截，听不到语音 | autoplay policy 需要用户手势 | 添加"🔊 点击播放语音介绍"按钮作为用户交互入口 |
| 页面介绍只在首页显示 | 其他页面没有引入 PageIntro 组件 | 全部 8 个页面都添加了 PageIntro |
| 百度 TTS per=4146 无效 | 用户示例代码写了 4146，但文档中该值不可用 | 改为用户最终指定的 per=4134 |
| 百度 TTS Content-Type 判断 | 成功返回 audio/*，失败返回 application/json | 检查 Content-Type 头区分两种响应 |
| 定位按钮点了没反应 | watcher 不更新 state，按钮读到的是旧位置 | 见 3.1（当前待修复） |
| 地图 light 样式报错 | API key 不支持该样式 ID | 已删除 mapStyleId='light' 代码 |

	---

## 八、✅ 已修复问题

### 8.1 【已修复】覆盖物无限重建导致资源耗尽

**根因：** watcher 回调中调用 `setUserLoc(loc)` 触发 useEffect 依赖链无限循环：
```
watchPosition 收到新坐标 → setUserLoc(loc) → React 重渲染
  → 标记 useEffect（依赖 userLoc）重建标记
  → watcher useEffect（依赖 userLoc）重启 watcher
  → 新 watcher 再收到坐标 → 无限循环
```

**修复方案（MapView / RoutePlanner / TripPlan 三个页面统一修改）：**
1. 新增 `userLocRef`（useRef）存储最新位置，不触发 React 重渲染
2. 用户标记创建 useEffect 只依赖 `[ready]`，创建一次永不销毁
3. watcher useEffect 改为依赖 `[ready, followMode]`，回调中只更新 ref + 直接调用 `setGeometries()`
4. 定位按钮直接调 `getCurrentPosition` + 用 ref 做降级回退

### 8.2 【已修复】定位不准 + 无授权提示

**修复方案：**
1. 定位按钮点击时触发 `getCurrentPosition` → 浏览器弹授权框
2. 拒绝权限 → 弹出遮罩提示："需要定位权限才能使用此功能"
3. 超时 / 失败 → 降级使用 ref 中缓存的最后已知位置

### 8.3 【已修复】分类图标不显示

**修复方案：**
1. Canvas 尺寸从 36×36 / 28×28 改为 64×64 / 32×32（WebGL 纹理友好尺寸）
2. MarkerStyle 同时设置 `color`（颜色回退）+ `icon`（自定义图标优先）
3. 新增 `getMarkerColor()` 导出每个分类的颜色值
4. 改进 Canvas 绘制：添加阴影效果提升视觉质感

### 8.1 【严重 · 需优先修复】覆盖物无限重建导致资源耗尽

**现象：**
- 地图标记闪烁
- 位置标记（蓝点）反复消失又出现
- 浏览器报 `ERR_INSUFFICIENT_RESOURCES`
- 定位按钮点了不生效

**根因：**
watcher 回调中加了一行 `setUserLoc(loc)`（为了定位按钮能读到最新位置），但这触发了 useEffect 的依赖链：
```
watchPosition 收到新坐标
  → setUserLoc(loc)
  → React 重渲染
  → 用户标记 useEffect（依赖 userLoc）重新执行
    → userMarkerRef.current.setMap(null)
    → new MultiMarker({...})    // 重建标记
  → watchUserLocation useEffect（依赖 userLoc）重新执行
    → 停止旧 watcher
    → 启动新 watcher
  → 新 watcher 再收到坐标 → 再触发 setUserLoc → 无限循环
```

每次循环都销毁并重建 MultiMarker、MultiCircle、MultiPolyline，WebGL 纹理不断分配释放，最终耗尽。

**修复步骤（三个页面都要改：MapView / RoutePlanner / TripPlan）：**
1. watcher 回调中**移除** `setUserLoc(loc)` → 不触发 React 重渲染
2. watcher 只通过 `setGeometries()` 直接更新标记位置（这是轻量操作）
3. 用户标记的创建 useEffect 只依赖 `[ready]`（不依赖 `userLoc`），创建一次永不销毁
4. 定位按钮自己独立调 `getCurrentPosition` 获取最新位置，不依赖 state

### 8.2 【严重】定位不准 + 无授权提示

**现象：** 
- 地图加载后定位到宽带运营商机房位置（河北）
- 点击定位按钮没有反馈

**修复方案：**
1. 先修 8.1（否则地图都炸了定位也动不了）
2. 用户点 📍 时，调用 `getCurrentPosition`（浏览器会弹出授权框）
3. 如果用户点"允许"：跳到精确位置 + 放大 16 级
4. 如果用户点"拒绝"或忽略：弹遮罩提示"需要定位权限才能使用此功能"
5. 如果浏览器不支持定位：提示"当前设备不支持精确定位"

### 8.3 【中等】分类图标不显示

**现象：** 地图标记仍然显示腾讯地图默认的红色大头针，不是分类图标（吃/玩/站/雨）

**尝试过的方案：**
- SVG data URI → 不工作
- SVG 文件路径 → 不工作
- Canvas 生成 PNG → 已实现 `src/utils/icons.ts`，但被 8.1 bug 干扰

**当前代码（`src/utils/icons.ts`）：**
```typescript
// Canvas 画圆 + 文字 → toDataURL('image/png')
// 每个类别缓存一次，后续复用
const cache = new Map<string, string>()
export function getMarkerIcon(category: string): string { ... }
export function getHouseIcon(): string { ... }
```

**待验证：** 修完 8.1 后，看图标是否正常显示。如果还是不显示，改用 `color` 属性 + `MultiLabel` 替代方案。

### 8.4 【低】页面引导不重复弹出

**现象：** 当前用 sessionStorage 记录已读状态，刷新页面后不弹。
**可选修复：** 设置页添加"重置引导"按钮，或改用每次挂载都弹。

### 8.5 【低】地图 light 样式
已删除 mapStyleId 代码，无残留问题。

---

## 九、文件结构（完整）

```
D:\秦皇岛之旅\
├── .env                              # 环境变量（4个 API 密钥）
├── .gitignore
├── CLAUDE.md                         # Claude 项目指令
├── index.html
├── package.json
├── tsconfig.json
├── vite.config.ts                    # Vite + React + Proxy
├── 历史记录.md                        # 本文件
├── public/
│   └── vite.svg
├── src/
│   ├── App.tsx                       # 6个路由配置
│   ├── App.css                       # Tailwind + 自定义样式
│   ├── main.tsx                      # 入口
│   ├── types.ts                      # Spot, Pitfall, TravelMode, PageIntroConfig
│   ├── vite-env.d.ts
│   ├── store/
│   │   └── tripStore.ts              # Zustand: filter, selected, wishlist, settings, order, mode
│   ├── data/
│   │   ├── places.ts                 # 32个景点 + 12个避坑 + 示例路线
│   │   └── pageIntros.ts             # 8个页面的语音引导配置
│   ├── services/
│   │   └── api.ts                    # loadTMap / suggestPlaces / searchNearby / getIpLocation / getUserLocation / watchUserLocation / chatDeepSeek
│   ├── utils/
│   │   ├── geo.ts                    # haversineDist / estDriveTime / estTaxiFare / estWalkTime / estRideTime / estTransitTime / decodeDirectionPolyline / generateCurvedPath
│   │   ├── icons.ts                  # getMarkerIcon / getHouseIcon（Canvas→PNG）
│   │   └── speak.ts                  # speak / stopSpeak / ensureVoice / getBaiduToken（百度 TTS + SpeechSynthesis）
│   ├── components/
│   │   └── PageIntro.tsx             # 全屏引导浮层（图标+标题+文案+语音播放）
│   └── pages/
│       ├── HomePage.tsx              # 首页
│       ├── MapView.tsx               # 总览地图（最复杂）
│       ├── RoutePlanner.tsx          # 路线规划
│       ├── TripPlan.tsx              # 行程管理（拖拽+优化+多段路线）
│       ├── PlaceGallery.tsx          # 地点库
│       ├── AIChat.tsx                # AI 助手
│       ├── PitfallArchive.tsx        # 避坑档案
│       └── AppSettings.tsx           # 设置
```

---

## 十、下次开始工作的标准流程

### 10.1 启动项目
```bash
cd "D:/秦皇岛之旅"
npx vite --host
# 打开 http://localhost:3002
```

### 10.2 修复优先序
1. **8.1 覆盖物无限重建** → 修改 MapView / RoutePlanner / TripPlan 的 useEffect 依赖链
2. **8.2 定位授权提示** → handleLocate 添加权限拒绝时的遮罩提示
3. **8.3 验证图标** → 8.1 修完后刷新看图标
4. **8.4 引导重置** → 可选优化

### 10.3 关键提醒
- `.env` 改了要重启 Vite 服务
- 腾讯地图 MarkerStyle 的 `color` 和 `icon` 是互斥的
- `setGeometries()` 是轻量更新，`new MultiMarker()` 是重建
- 用户不在秦皇岛本地（在山西），定位测试时要注意区分
